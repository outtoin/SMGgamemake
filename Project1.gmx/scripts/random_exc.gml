/// random_exc(firstnum, secondnum, range1, range2)

// get random number except given range of number

var result = 0;
var num;
while (true) {
    num = random_range(argument0, argument1)
    if (inRange(num, argument2, argument3)) break;
}

return num;

